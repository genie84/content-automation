# 콘텐츠 자동화 파이프라인 (content-automation)

## 프로젝트 목표
유튜브 채널/뉴스 소스의 콘텐츠를 자동 수집 → 재가공 → 블로그(워드프레스) 자동 발행.
이후 블로그 포스팅을 기반으로 인스타그램 카드뉴스, 유튜브 쇼츠까지 확장하는 3단계 파이프라인.

- **1단계**: 소스 수집 → 재가공 → 블로그 자동 포스팅 (현재 작업 범위)
- **2단계**: 블로그 포스팅 → 인스타그램 카드뉴스 (추후)
- **3단계**: 콘텐츠 → 유튜브 쇼츠 (추후)

## 운영 원칙
1. **수익화가 최우선 목표.** "비용 0원"은 절대 원칙이 아니라 기본 방향. 무료로 되는 건 무료로 하되, 결과물을 보고 비용 대비 효과가 있으면 유료 전환도 검토한다.
2. **텍스트 재가공은 Gemini API(무료 티어)로 시작.** Anthropic API는 현재 QA 프로그램(교원그룹 프로젝트)에서 사용 중이며, 비용 구분을 위해 이 프로젝트에서는 당분간 사용하지 않는다. 품질 보정이 필요해지면 그때 별도 키 발급을 재검토한다.
3. **확장 가능한 구조로 설계할 것.** 지금은 삼프로TV 하나만으로 파이프라인을 완성하지만, 코드는 "채널 하나 전용"이 아니라 "소스 목록(config)을 순회 처리"하는 구조로 짤 것. 나중에 채널 추가 시 설정값만 추가하면 되도록.
4. **저작권/AI 말투 주의.** 유튜브 채널명·출연진 이름을 본문에 노출하지 않는다. "제공해주신...", "원고를 작성해 드립니다" 같은 AI스러운 문구 금지.
5. **저작권/부정경쟁방지법 리스크 관리 (중요, 1-5 프롬프트 설계 시 반드시 반영).**
   - 자막 내용을 순서대로 요약하거나 문장을 살짝만 바꿔 옮기는 방식은 금지. 저작권법상 '창작성' 부족으로 침해 판단 위험이 있고, 매일 자동·반복 처리되는 구조라 부정경쟁방지법 제2조 제1호 (파)목(타인의 상당한 투자·노력으로 만든 성과의 무단 사용)에도 해당될 수 있음.
   - 영상에서 다룬 주제/사실관계만 소재로 가져오고, 글의 구조(도입-전개-결론)·예시·비유·"서현이 아빠 생각" 콜아웃은 완전히 새로 작성할 것.
   - 가능하면 해당 영상 하나만 보고 쓰지 말고 관련 뉴스/자료를 함께 참고해 종합하는 형태로 구성 (그 영상의 재가공물이 아니라 해당 이슈에 대한 독자적 칼럼에 가깝게).

## 블로그 페르소나 (기존 "서현이아빠의 경제공부" 블로그와 동일 톤 유지)
- 40대 아빠 블로거(서현이 아빠), 비전문가, 독자와 함께 공부하며 후배에게 설명하듯 작성
- 항상 밝고 유쾌한 톤, 트렌디한 한국어 유행어를 살짝 어색하게 섞어 씀(아저씨가 애써 따라하는 느낌, 스스로도 약간 쑥스러워하는 뉘앙스)
- 절대 전문가인 척하거나 잘난 척하지 않음

## 기술 스택
- **언어/실행환경**: Python + GitHub Actions (매일 아침 자동 실행, 서버 유지비 없음)
- **텍스트 재가공**: Gemini API (무료 티어, Flash 모델)
- **발행 대상**: 워드프레스(REST API), 사이트 주소 `https://lampgenie.co.kr` (2026-08-30 정식 도메인 연결 완료, 기존 `yahaho1004.mycafe24.com`에서 전환)

## 이미 준비된 것 (GitHub Secrets에 등록 완료)
- `GEMINI_API_KEY` — Gemini API 키
- `WP_USERNAME` — 워드프레스 로그인 아이디 (yahaho1004)
- `WP_APP_PASSWORD` — 워드프레스 REST API용 애플리케이션 비밀번호 (이름: content-automation)

## 파이프라인 흐름 (1단계 목표 구조)
1. RSS 수집: 유튜브 채널(우선 삼프로TV) RSS 피드에서 신규 영상 확인
2. 원문 추출: 영상 자막/설명 텍스트 확보
3. 재가공: Gemini API 호출, 아래 JSON 스키마로 응답 강제
   ```json
   {
     "title": "매력적인 제목",
     "summary_lines": ["핵심요약1", "핵심요약2", "핵심요약3"],
     "table_rows": [{"항목": "...", "현재": "...", "리스크": "..."}],
     "body_html": "<h2>...</h2><p>...</p> 형식 본문"
   }
   ```
4. HTML 조립: 요약박스(div) + 표(table) + 본문(body_html) + 유튜브 반응형 임베드(iframe)
5. 워드프레스 REST API로 자동 발행
6. GitHub Actions로 매일 아침 자동 실행 스케줄링

## 소스 목록 설계 (확장성 고려)
```python
SOURCES = [
    {
        "name": "삼프로TV",
        "rss_url": "https://www.youtube.com/feeds/videos.xml?channel_id=UChlv4GSd7OQl3js-jkLOnFA",
        "tone_note": "경제 전문가 인터뷰형",
    },
    # 향후 채널 추가 시 여기에 딕셔너리만 추가
]
```

## 참고: 스프린트 체크리스트
프로젝트 진행 상황은 `콜품질평가AI_스프린트체크리스트` 형식과 동일하게 별도 엑셀 파일로 관리 중 (담당: 서현이 아빠 / Claude 구분, 완료여부 Y/N, 비고에 날짜·세부사항 기록).
Claude Code에서 작업 완료 시, 해당 항목을 Y로 체크하고 비고에 구현 내용을 기록하는 방식으로 진행할 것.

## 지금 시작할 작업 (스프린트1)
1-1. 유튜브 채널 RSS 피드 목록 확정 (삼프로TV)
1-3. RSS 수집 스크립트 작성
1-4. 유튜브 자막/설명 추출 스크립트 작성
1-5. Gemini API 연동 및 재가공 프롬프트(JSON 스키마) 작성
1-6. HTML 조립 로직 구현
1-7. 워드프레스 자동 포스팅 연동
1-9. GitHub Actions 스케줄 설정
